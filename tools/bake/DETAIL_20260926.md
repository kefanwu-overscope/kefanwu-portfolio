# Studio furnishing rebake · 2026-09-26

This revision keeps the accepted room geometry, original UV atlas, light rig,
RGBM16 encoding, and irradiance/π shader contract from `REALISM_20260925.md`.
The new browser export supplies the current printer, tools and other static
furnishings. Project hotspot descendants stay outside the bake because the
room can rotate those models; they continue receiving live lighting.

Use the actual `static-casters.glb` exported after the furnishing changes pass
local review. Do not substitute the previous export or use preview output as
a completed bake. All masters stay outside the deployed site in
`../.codex/room-detail-20260926/`; the previous accepted source directory remains
unchanged.

From the site root in PowerShell:

```powershell
& 'C:/Users/oc/.cache/blender/blender-4.5.9-windows-x64/blender.exe' --background --python tools/bake/realism_bake.py -- --root . --evidence ../.codex/room-detail-20260926/bake-final --casters ../.codex/room-detail-20260926/static-casters.glb --resolution 4096 --samples 256 --states off on --version detail-20260926 --seed 20260925 --deploy ../.codex/room-detail-20260926/encoded

& 'C:/Users/oc/.cache/blender/blender-4.5.9-windows-x64/blender.exe' --background --python tools/bake/bake_desk_detail.py -- --source ../.codex/room-detail-20260926/bake-final --output ../.codex/room-detail-20260926/desk-detail --source-resolution 4096 --resolution 512 --samples 256

python tools/bake/encode_realism.py --evidence ../.codex/room-detail-20260926/bake-final --output ../.codex/room-detail-20260926/encoded --source-resolution 4096 --resolution 2048 --range 16
python tools/bake/encode_realism.py --evidence ../.codex/room-detail-20260926/bake-final --output ../.codex/room-detail-20260926/encoded --source-resolution 4096 --resolution 4096 --range 16
python tools/bake/encode_desk_detail.py --source ../.codex/room-detail-20260926/desk-detail --output ../.codex/room-detail-20260926/encoded

node tools/bake/package_realism.cjs --input ../.codex/room-detail-20260926/encoded --output models/baked/detail-20260926 --evidence ../.codex/room-detail-20260926 --geometry models/baked/realism-20260925/room-baked.a318939777a14e42.glb --previous-initial-bytes 3290306 --prepare-only
```

The Python encoder requires NumPy and Pillow. On this workstation its bundled
executable is
`C:/Users/oc/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe`.

The two 4096² atlases use 256 Cycles/OptiX samples and six diffuse bounces;
the probes are 512×256 at 64 samples. The separate 512² desktop receiver uses
256 samples. The 2K atlas is an exact scene-linear area downsample before
encoding. Texture alpha is data: preserve straight alpha, `NoColorSpace`,
`flipY=false`, and no generated mipmaps. Never re-encode these PNGs through a
lossy image pipeline or a canvas that premultiplies alpha.

`--prepare-only` writes eight content-hashed image files to the new runtime
directory and reuses the existing immutable geometry URL. It writes these
review artifacts to the evidence directory without changing site integration:

- `experience-baked-assets.js` and `runtime-assets.json`: proposed runtime URLs.
- `immutable-headers.json`: cache entries to merge into `vercel.json`.
- `packaged-assets.json`: exact hashes, byte counts, bake settings and caster hash.
- `asset-commit-files.txt`: only the eight new deployed assets.

After reviewing the actual bake and browser output, rerun the same packaging
command without `--prepare-only` to apply the module and immutable cache rules.
`--check-only` checks all package inputs without writing any files. Existing
cache entries and legacy assets are preserved. The source manifests establish
the completed bake; the commands above are the reproduction recipe.

Compare `packaged-assets.json.initialBytes` against the previous 3,290,306-byte
initial geometry + off-state 2K atlas + off probe + desktop package. This count
does not include the exhibit models, other furnishing downloads, browser decode
time, or GPU memory. Keep 4K and the on-state resources demand-loaded.

## Completed source and prepared package

The actual Blender 4.5.9 LTS / RTX 5080 run completed both states using 404
static caster objects from the final 31,032,496-byte browser export. Its SHA-256
is `3a1651395a66a7b1312006d4ffa21eef73e0a8c7f3f1d6693e48ca77ee8d7162`.
`geometry-validation.json` passes all 12 checks, including identical source
geometry, UV1, atlas partitions and light rigs. The runtime room GLB remains
`a318939777a14e42a6acd3fb484f84d9721029428392bf1ad98542f7164c3b82`.

The prepared initial package is 3,237,693 bytes, 52,613 bytes (1.60%) below the
previous package. The eight new assets total 18,188,416 bytes including optional
4K and on-state files. `encoded-validation.json` checks all nine runtime file
hashes and decodes all six PNGs against their float sources: zero clipped
channels, no color profiles, and every channel within half its RGBM
quantization step. Both Cycles proofs and the desktop contact-shadow preview
were visually reviewed. Browser integration and release remain separate checks.
