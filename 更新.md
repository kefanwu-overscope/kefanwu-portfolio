# 网站更新记录

## 2026-09-26 · 3D 项目返回时保留房间

从3D房间打开项目后保留原场景和已解码模型，项目介绍仍使用完整页面。
返回按钮直接恢复房间，不再重新打开experience.html或播放启动加载；
相机、灯光与2K/4K选择保持。项目打开期间暂停房间渲染，关闭时释放项目资源。

统一管理章节和下一项目的浏览器历史，支持连续浏览后的返回、前进/后退，
并恢复键盘焦点。直接打开项目链接或刷新项目页仍使用独立页面和正常返回。
16个项目真实往返保持同一场景/文档，返回没有新增房间模型请求；手机与
三个返回入口通过。实现与测试说明见 STUDIO_NAVIGATION.md。

## 2026-09-26 · 3D 房间模型同步与工作台细化

房间的16个项目统一采用主站当前动画的起始模型和材质，替换旧版材质分组、
FTC简化替身和CFD图片显示器。补上工作台上的Guitar Education Kit；Vine的
初始薄膜、材料测试夹具/橙色试样、碳纤维源纹理等同步。Pool Sniper与
LineFollower沿展架长度摆放，避免因朝向挤入柜体深度而过度缩小。

打印机增加导轨、丝杆、滑块、皮带、热端/喷嘴、走线、带孔线盘、铰链和通风口。
工具细化钳口、转轴、套筒孔、刻度、握柄和挂架；更新电源、焊台、万用表等仪表。
保留原房间布局和交互，完整项目页、工程资料、主站封面与动画数据不改。

实际新装饰物完成Blender/Cycles日夜预烘焙：4096²、256采样、6次漫反射反弹，
含室内探针与桌面细节图；默认2K、按需4K。16个房间模型合计6.145MB压缩数据，
不用下载完整动画。初始资源本地测量从21.98MB降到21.44MB；10Mbps/40ms单机
测试加载基本持平。详情、全部模型改动和测试限制见 STUDIO_ROOM_DETAIL.md。

## 2026-09-25 · 返回导航与预烘焙场景美术

主站项目页加入醒目的「← Back to projects」，3D项目页加入「← Back to studio」。
按钮随顶部导航保持可见；手机使用两行导航，320/390px无横向溢出。返回链接
不依赖浏览历史，直接进入项目页或新标签页也能正确返回，3D返回继续恢复视角、
灯光与2K/4K选择。

房间使用Blender 4.5.9 / Cycles / OptiX重新预烘焙：原生4096²、256采样、
六次漫反射反弹，包含柜体、工作台、椅子和静态物件的遮挡与接触阴影；提供
由线性光照下采样的默认2K与按需4K，更新两种灯光状态的室内反射探针。
桌面增加512²独立细节烘焙，改善台灯光斑和桌垫周围的接触阴影。

浏览器使用无损RGBM图像保存HDR光照，校准后只应用一次烘焙漫反射，保留
实时/环境镜面反射。减少大面积白色表面的泛光，并修正柜体粉末涂层粗糙度与
背板木纹法线强度。保留已确认的布局、原模型几何/UV和全部项目交互。

默认烘焙相关资源（房间几何、夜间2K图、反射探针、桌面细节）从7.31 MB降至
3.29 MB，减少55%。同浏览器/视口、禁HTTP缓存的六次本地交替测量未发现加载
回退；这是单机测试，不代表所有设备与网络。光照编码、实际GPU解码、画质切换、
返回流程均已验证。源场景与复现说明见 tools/bake/REALISM_20260925.md。

## 2026-09-20 · 3D 项目介绍与主站统一

16个3D项目页改为与主站case-study一致的完整长页，共用同一渲染器、资料和
case-study.css。标题、摘要、64个章节、94张图片、79条补充资料、技术记录、
下载与Javelin的35行BOM保持主站原文及顺序，不再只显示侧栏摘选内容。

页首保留真实3D旋转、缩放、播放/倒放、时间轴、复位和标准视角；3D模式使用
独立控制器嵌入主站预览位置，不再额外加载主站的WebP动画。延续压缩资源、
分阶段加载和缓存优化。模型离开视口、页面隐藏或打开图片时暂停绘制。

项目图片共用主站的裁切、完整CAD/图表留白、去重与放大浏览。旧的
project-3d.html#项目key链接自动转为?project=项目key，保留章节锚点。
返回3D房间仍恢复原视角和灯光，主站普通case-study页面的内容与样式保持原样。

## 2026-09-19 · 3D 加载与渲染性能优化

保留恢复后的工作室布局、灯光与16个独立项目工作台。全部项目压缩包从
97.68 MB降至38.75 MB（约60.3%）；保留所有三角形、材料、动作采样及已确认
的工程数据。通过精确顶点复用、受限16位显示量化、差分与字节分组压缩缩小
传输；碳纤维位置与织纹坐标保持源Float32精度，铺层仍不增加座椅厚度。

- 工作台先加载可旋转的初始姿态，随后流式下载、解压动画；Vine初始包约
  0.89 MB，Mk.7座椅约0.11 MB。初始姿态与完整动画第零帧逐位匹配。
- 合并共同运动和材质相同的零件、复用碳布缓冲、优化形变插值与进度UI，
  保留转向零件点选、正反放、随机拖动、标准视角及所有机构运动。
- 房间模型请求有限并行、解析分步进行；重用静态阴影、合并静态物件、
  减少静止重绘，按真正的后处理目标预编译材质。原开场镜头可跳过或直接
  接管，不强制等待演出结束。
- Three.js原版本文件改为同站托管，依赖提前加载；项目包使用内容哈希地址
  与一年缓存，旧版资源保留兼容。项目工作台不再加载房间样式表。

本机初测房间静止主线程占用约47%→14%单核，Vine完整播放约8秒的主线程
CPU用时约4.47→1.21秒；这些不是GPU帧率或所有设备的性能保证。完整资源、
动作、取消、重试、返回状态及桌面/窄屏验收见 [STUDIO_PERFORMANCE.md](STUDIO_PERFORMANCE.md)。

## 2026-09-19 · 恢复旧版房间，保留新项目工作台

按用户确认，3D 房间布局、灯光、首次进入镜头、顶部导航、底部项目选择、
简历纸张交互和首页工作室入口恢复 `69ee72c`。新 3D 项目工作台保留，独立入口
改为 `project-3d.html#项目key`，16 套动画、资料和控制不变。

旧版展品点击进入保留的工作台；返回工作室时恢复离开前的视角和灯光。
旧的 `experience.html#项目key` 地址仍直接进入对应工作台，图文详情页链接已同步。
此次保留全部动画源资源；不撤销此前确认的材料、技术数据、BOM或图片。

## 2026-09-19 · 3D 工程工作室 v2

已完成实现与运行时验收。对照基线为 `69ee72c`；架构和恢复使用说明见
[STUDIO_V2.md](STUDIO_V2.md)。精确发布状态以本次备份目录中的
`release-metadata.json` 与 `deployment-verification.json` 为准。

- 新增独立模型工作台，与房间浏览分开。16 项均可由封面目录、分类、搜索和
  `experience.html#项目key` 直接进入；Education 新增房间展品，FTC 房间展品
  复用已有重建源。直接进入项目时不加载整个房间。
- 16 套动作从已有 Blender 几何与已确认控制器导出，支持旋转、缩放、正放、
  倒放、暂停、进度拖动、复位与标准视角。进度只在时间轴区域接受滚轮；
  模型区域滚轮负责缩放。转向机构可点选查看传动路径说明。
- 保留中性白光、`#191919` 深灰舞台及零件自身颜色。桌面资料位于侧栏，手机
  模型与控制在上、资料可折叠。加入封面过渡、失败重试和独立资料入口。
- 标题、责任/范围、结果/目标与图片继续共用主站数据，不把设计目标改称实测
  结果。79 条补充字段、94 张图库引用和 Javelin 35 行 BOM 保持原记录；完整
  案例、BOM、技术记录与简历有普通链接入口，CAD 和图表完整包含显示。
- 快速切换取消旧请求，房间与工作台仅活动视图绘制；资源按需加载并在淘汰时
  释放。缓存最多两项，手机/桌面托管数组预算为 100/192 MiB，允许单个超预算
  当前项目独占缓存；这不是浏览器总内存上限。

动作范围覆盖转向联动、vine 外翻、Javelin 螺旋桨与展示飞行、扫描龙门、刹车
示意升温、AURA/FTC 拆装、碳布贴合、Mk.7 座椅折叠、试样示意拉断、已有 CFD
路径运动、Pool 回缩释放、LineFollower 轮组运动、Smelly 龙门、Telecaster
展示自转和 Education 吉他组装。主站原预渲染 WebP 动画保持不变。

碳纤维十个视觉阶段仅作工艺示意；实际为 EL2、20 层 3K 200 g/m² 斜纹主体加
5–10 层局部增强。每片布贴合固定源壳体，完成后不累积厚度。CFD 使用已有
Fluent 求解和数值路径，压力场保持稳态，并保留原非线性色标与定性解释边界；
本次没有重跑 Ansys。WebGL 的 PBR、噪声和织纹着色是源材质的交互实现，不承诺
与 Cycles 逐像素一致，也不新增工程精度结论。

验收记录：`../.codex/studio-v2-20260919/browser-runtime.json` 中 16 项全部通过；
`../.codex/studio-runtime-20260919/runtime-results.json` 中 39 项检查通过，覆盖
正反向及随机 seek、源端点、横竖屏动作包围范围、取消、缓存、释放、重试与
gzip 解码。浏览器测试入口为 `tools/studio-tests/index.html?run=1`。

这些是本地检查结果。记录的加载耗时和 CPU 绘制样本不构成同条件线上性能
对比，也不代表 GPU 帧时或 FPS 保证；生产发布及线上字节核验仍需单独记录。
